{
  "type": "object",
  "required": ["library"],
  "properties": {
    "library" : {
      "type" : "array",
      "required" : [
        "id",
        "quality",
        "expiration_date",
        "content_id",
        "time_to_expire"
      ],
      "properties" : {
        "id": {"type": "integer"},
        "quality": {"type": "string"},
        "expiration_date" : {"type": "datetime"},
        "content_id": {"type": "integer"},
        "time_to_expire": {"type": "string"}
      }
    }
  }
}
