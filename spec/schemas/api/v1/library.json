{
  "type": "object",
  "required": ["library"],
  "properties": {
    "library" : {
      "type" : "array",
      "required" : [
        "id",
        "quality",
        "expiration_date",
        "content_id"
      ],
      "properties" : {
        "id": {"type": "integer"},
        "quality": {"type": "string"},
        "expiration_date" : {"type": "datetime"},
        "content_id": {"type": "integer"}
      }
    }
  }
}
